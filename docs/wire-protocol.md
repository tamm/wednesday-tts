# Wire Protocol

**This document is superseded by [`voice-pipeline-spec.md`](voice-pipeline-spec.md).**

The spec contains the full JSON wire protocol schema, voice resolution rules, and pipeline definition. All hooks and the daemon now use JSON — the old colon-delimited `SEQ:` format has been removed.
